
# We get the info from the user
length = int(input("Insert the length: "))
width = int(input("Insert the width: "))

# We calc this and sent the result
print(f"Perimeter: {2 * (length + width)}")
print(f"Ekef: {length * width}")